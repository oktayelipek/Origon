name: origon_ui
description: Origon UI — Flutter component library.
version: 0.0.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  origon_tokens:
    path: ../tokens-flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
