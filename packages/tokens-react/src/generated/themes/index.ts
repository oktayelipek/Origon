// GENERATED by tokens/build.config.mjs. Do not edit by hand.
// Regenerate with: pnpm build:tokens

import { tokens as kriptoDark } from './kripto-dark';
import { tokens as kriptoLight } from './kripto-light';
import { tokens as hisseDark } from './hisse-dark';
import { tokens as hisseLight } from './hisse-light';
import { tokens as globalDark } from './global-dark';
import { tokens as globalLight } from './global-light';

export type Brand = "kripto" | "hisse" | "global";
export type Theme = "dark" | "light";

export const themes = {
  'kripto-dark': kriptoDark,
  'kripto-light': kriptoLight,
  'hisse-dark': hisseDark,
  'hisse-light': hisseLight,
  'global-dark': globalDark,
  'global-light': globalLight,
} as const;

export type ThemeKey = keyof typeof themes;

export function getTokens(brand: Brand, theme: Theme) {
  return themes[`${brand}-${theme}` as ThemeKey];
}

/** Applies a brand/theme by setting data-* attributes on document.documentElement. */
export function applyTheme(brand: Brand, theme: Theme) {
  if (typeof document === 'undefined') return;
  document.documentElement.setAttribute('data-brand', brand);
  document.documentElement.setAttribute('data-theme', theme);
}
